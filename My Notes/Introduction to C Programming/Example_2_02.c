#include<stdio.h>

void main()
{
    int file_manager;
    int file_management;
    file_manager = 5;
    file_management = 10;
    printf("Iam the variable 1 : %d\n",file_manager);
    printf("Iam the variable 1 : %d\n",file_management);
}

/* 
Note : The identifiers file_manager and file_management are both grammatically valid. Some compilers may be unable to distinguish between ther,
because the first eight letters are the same for each identifier. Therefore. only one of these identifiers shoulid be used in a single C program.

*/